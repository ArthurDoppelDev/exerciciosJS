const pontuacao = 0;

const nivel = pontuacao =>1000 ? `VIP`:`Comum`;

// if (pontuacao >= 100){
//   const nivel = `VIP`;
// }else{
//   const nivel = `Comum`
// }

//EXTRA valores padrão

const userTheme = null;
const theme = userTheme || `Dark`;
